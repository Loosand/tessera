# AI 对话与工作区 Agent 实施 TODO

> 代码源头：`packages/ai/src/server/ai-sdk-runtime.ts`、`packages/ai/src/react/ai-provider-settings.tsx`、
> `packages/agent-runtime/src/index.ts`、`packages/skills/src/index.ts`、`packages/contracts/src/index.ts`、`packages/database/schema.ts`、
> `apps/desktop/src/main/index.ts`、`apps/desktop/src/renderer/src/components/tasks/conversation/task-page.tsx`、
> `apps/desktop/src/renderer/src/components/tasks/messages/chat-parts/`、`packages/ai/src/react/use-electron-chat.test.ts`
>
> 状态：部分实现。本文记录实施顺序与验收门槛；勾选任务时必须同步对应代码、测试和架构文档。

> 决策更新（2026-09-01）：M2/M3 以下保留早期交付记录；新运行已在 P1 切换到无需逐次文件审批的
> `read/edit/write`，P2 又移除了内容/研究领域工具和阶段状态机，P4 在 macOS 隔离探针通过时增加受控 `bash`。
> 旧勾选项只证明历史能力曾完成，不代表仍向新模型开放。
> 当前实施顺序以 [Pi 参考下的 Agent 减法实施路线图](agent-simplification-roadmap.md)为准。

## 目标

从现有「新任务」界面出发，先交付四层安全基础，再收敛为统一创作运行时：

1. 安全、可持久化的供应商运行时。
2. 不读取工作区的普通 AI 对话。
3. 可读取工作区但不可修改文件的 Agent。
4. 通过 Diff、人工批准和审计记录修改 Markdown 的 Agent。
5. 不区分 Chat/Agent 的统一 `ToolLoopAgent`、每轮动态 Skill、动态资源和后端无关的项目/文档领域工具。

这个顺序是依赖关系，不是五套独立产品。当前独立任务与工作区任务都已使用同一个 `ToolLoopAgent`；历史 mode
只保留兼容归属，当前是否注入工作区工具由眼下打开且已授权的工作区决定；
目标交互只保留一个自然对话入口，每轮运行自行决定直接回答或调用工具。内容存储仍处于探索阶段，暂以托管内容库
混合方案验证流程，同时保留数据库正文和完全开放外部工作区候选。详见
[统一创作 Agent 与内容存储探索](unified-creation-agent.md)。

## 当前基础

| 能力 | 状态 | 现状 |
| --- | --- | --- |
| 供应商适配 | 部分实现 | `@tessera/ai` 已能创建 OpenAI 兼容、Anthropic 兼容、DeepSeek、Grok 和 OpenRouter 模型；兼容协议允许多条具名连接，官方服务保持单例；DeepSeek 官方 Anthropic Web Search 固定使用其 Harness 已验证的 `web_search_20250305`，并用无 thinking 的复杂搜索与本地工具续轮规避已复现的供应商兼容错误。 |
| 供应商配置 | 部分实现 | API Key 由系统安全存储加密，Base URL、启用状态和手动模型持久化；模型目录是可选能力，404/405 不判定推理连接失效。 |
| 新任务界面 | 部分实现 | 只暴露自动/研究/写作/问答与模型，不显示 Chat/Agent、联网或思考开关；已接入流式消息、停止、恢复、结构化问题、事实 Progress、脱敏 Execution Context、Artifact 关系/预览、文件跳转，并兼容显示旧研究计划和 Diff。 |
| Skill 基建 | 部分实现 | 标准 `SKILL.md`、轻量研究/写作内置注册表、用户导入、逐轮显式选择与 AI SDK `instructions` 已接通；自动方式已保守识别明确研究/写作意图，更新/版本与更强语义路由待实现。 |
| Agent 协议 | 部分实现 | 所有任务使用同一 AI SDK `ToolLoopAgent` 与动态 call options；按当前资源注入可选 Web、工作区 read/edit/write、macOS 探针后的受控 bash 和逐次审批 MCP，内容/研究领域工具已退出。durable 自动续跑未接入。 |
| 文档侧栏 | 已实现 | 文档右侧栏直接复用 `TaskPage`、任务 ID、IPC、持久化与恢复链路；当前 Markdown 最新草稿默认显示为可移除附件，发送后在主进程边界转成受限文本上下文。 |
| 文件读写 | 已实现 | 新运行使用 `read/edit/write`，macOS 隔离可用时增加 `bash`；主进程限制直接文件工具的路径和类型，精确 edit、hash 复核、同文件队列与原子提交，Bash 由 ExecutionEnvironment 限制工作区/网络/Secret/进程组/输出，普通工作区操作不逐次审批；旧 approval 只读兼容且不写盘。 |
| 会话数据 | 部分实现 | 会话/消息、逐轮 RunPolicy、资源摘要、运行事件/指标、规范化资源关系、Artifact 和 Operation 已保存；Progress/Execution Context 从这些事实按需重建，旧 mode/workspace 只做兼容。 |
| 内容存储 | 部分实现（探索） | 托管内容库 + SQLite 控制层原型已能创建未归档文档、项目和安全移动，同时保留外部工作区；最终仍与数据库正文、完全外部工作区比较。 |

## 固定边界

- 渲染层不持有供应商密钥，不直接请求模型，也不直接访问文件系统。
- 每轮默认只发送用户显式输入、显式附件和当前会话历史，不读取未附加的工作区、当前文档或剪贴板。
- 当前实际 Skill 通过 AI SDK instructions 注入，只改变执行方法；联网、资源读取与写入能力由本轮作用域、创作方式和请求期模型端点共同解析，Skill 声明本身不授予权限。
- 当前文件适配器的工作区根目录由主进程注入，模型不得通过参数自行选择或扩大目录；未来数据库适配器也必须使用稳定资源 ID 和相同授权语义。
- `read/edit/write` 只在当前授权工作区内操作 Markdown；macOS 探针通过时的 Bash 只能按本轮策略读或写当前工作区，不继承宿主 Secret且无网络。删除、重命名仍不是独立模型工具；供应商原生联网与 MCP 是独立外部能力，MCP 只从显式信任/启用的服务器注入并逐次审批。
- 可写 Agent 第一版只创建或更新 Markdown，不删除或重命名文件。
- 文件更新必须绑定最近读取的 hash；冲突和取消不得覆盖磁盘新版本。需要审查时由运行事件和文件历史提供 Diff，不再以逐次审批阻断普通编辑。
- 每次工具调用设置路径、结果大小、步骤数、时间和 token 上限。
- 探索阶段继续以 Markdown 为事实源，数据库只保存会话、索引、运行状态和审计；最终存储评审前不得引入正文双写或不可逆迁移。

## 目标数据流

```text
用户输入与本轮创作提示
  -> 渲染层消息界面
  -> 类型化 IPC
  -> 主进程解析 RunPolicy、供应商、模型、Skill、资源和工具
  -> AI SDK ToolLoopAgent（按当前授权资源动态装配工具）
  -> 文本增量或工具事件
  -> 渲染层呈现

可写 Agent 的文件提案
  -> 主进程冻结路径、基准版本和候选内容
  -> 渲染层展示 Diff
  -> 用户批准或拒绝
  -> 主进程重新校验磁盘版本
  -> 原子写入或报告冲突
  -> 记录审计事件
```

## M0：供应商运行时

目标：用户配置一次供应商后，重启应用仍可选择模型；后续对话和 Agent 不再各自处理密钥。

### 配置与存储

- [x] 定义供应商配置契约：供应商类型、显示名称、Base URL、默认模型、启用状态和凭据引用。
- [x] 在主进程使用系统安全能力加密保存 API Key；数据库或配置文件只保存非敏感字段和凭据引用。
- [x] 增加读取、保存、删除、检查模型目录和列出可用模型的窄 IPC；返回渲染层的对象不得包含完整 API Key。
- [x] 将设置页中的连接草稿接入持久化配置状态，并区分保存、目录检查和目录不可用；目录不可用不代表推理连接失败。
- [x] 默认选择只保存 `configId::modelId`，配置失效时明确要求用户重新选择，不静默切换供应商。

### 运行时

- [x] 在主进程增加统一的模型解析器，根据配置创建 AI SDK `LanguageModel`。
- [x] 请求正文、Header、工具载荷与堆栈不进入产品诊断；供应商错误响应正文最多保留 16,000 字符，只剔除 API Key / Authorization 凭据并通过按需 Run Inspector 展示。
- [x] 开发环境使用 AI SDK 官方 Telemetry / DevTools 记录 Agent run、step、tool、usage 与 timing，并通过设置页窄 IPC 按需打开本地 Viewer；生产脱敏日志仍独立规划。
- [x] 模型启动、流式与恢复失败使用版本化公开错误，区分配置、认证、限流、超时、不可用、网络与中断；公开提示保持稳定，供应商错误响应正文单独进入有界诊断。DeepSeek Anthropic 路由使用官方稳定 `web_search_20250305`，显式关闭并剔除 thinking，以两次搜索和本地工具混合续轮测试约束第二次请求。工具与 IPC 的完整统一仍归入横向任务。

### 验收门槛

- [x] 保存供应商、退出并重启应用后，启用状态、Base URL、模型列表和默认模型可恢复。
- [x] 用户可以从已配置供应商直接选择模型；未配置状态有明确入口，不展示伪造的可用模型。
- [x] 渲染进程状态和 IPC 响应中不包含完整 API Key；生产诊断不记录秘密，开发 DevTools 的明文边界单独显式说明。
- [ ] 使用模拟供应商覆盖成功、超时、限流、鉴权失败和畸形响应测试。

## M1：普通 AI 对话

目标：把「新任务」从本地输入界面接成可停止、可恢复的多轮流式对话，同时保持良好的手动输入和编辑体验。

### 流式协议

- [x] 在 `packages/contracts` 定义开始、文本增量、完成、失败和取消事件，包含稳定的会话 ID、消息 ID、请求 ID 和递增序号。
- [x] 在主进程使用 AI SDK `ToolLoopAgent` 发起流式生成，并用 `AbortController` 实现显式停止和窗口关闭清理。
- [x] 为 React `useChat` 实现 Electron `ChatTransport`；渲染层不使用会直接请求外网的 transport。
- [x] 处理重复事件、乱序事件、恢复缺口、订阅释放、渲染进程刷新和幂等中途取消，避免消息重复、静默关闭或请求悬挂。
- [x] 使用合成 `start → text-start → text-delta → text-end → finish` 序列验证 AI SDK React 消费者逐段收到正文，结束后 Part 进入完成状态。
- [x] 透传供应商明确返回的 `reasoning-start → reasoning-delta → reasoning-end`，按原始 Part 顺序用可折叠、紧凑 Markdown 过程块展示，不合成伪思维链。
- [x] 供应商只有 reasoning start/end 而没有 delta 时隐藏完成态空块，不用占位文案冒充可展示摘要。

### 会话与界面

- [ ] 提交首条消息后，将居中的任务输入框平滑转换为消息列表和底部固定输入框。
- [ ] 支持多行输入、选区编辑、撤销、粘贴、中文输入法、键盘发送与鼠标发送；流式生成期间仍允许整理下一条输入。
- [x] 正文、reasoning 与 Agent 结果预览统一使用 Streamdown，支持 GFM、CJK、Shiki、KaTeX、Mermaid 与流式未闭合语法修复；逐字节奏由外层尾部单调缓冲控制，Streamdown 不重播字符动画；代码、表格和图表提供复制、下载、全屏等适用控件，工作区 Markdown 引用继续可跳转，原始 HTML 与危险协议保持禁用。
- [x] 将 reasoning、正文、来源和工具调用拆为独立 Part 呈现；reasoning 默认展开、可折叠并以紧凑密度复用正文 Streamdown 渲染器，正文使用 SDK Part 状态启用尾部缓冲、增量光标和未闭合语法修复，工具状态不混入 Markdown。
- [x] 消息滚动只在用户贴底时自动跟随；离开底部后保留阅读位置并显示返回最新消息按钮。
- [ ] 明确显示当前供应商与模型；切换模型只影响下一次生成，并记录到对应消息。
- [x] 确定通用任务会话模型：普通对话允许没有工作区，Agent 会话必须绑定工作区；通过前向迁移落地，不重写已有迁移。
- [x] 持久化消息内容、reasoning/来源/工具等消息 Part 以及供应商和模型；应用重启后可恢复会话。
- [x] 将流运行失败写成带稳定 code、phase 与 retryable 的版本化 `data-task-error` 消息 Part，保留已经收到的助手正文、公开错误与消息内重试入口；
  输入框不再重复显示同一运行错误，重启后失败状态仍可从消息恢复。
- [x] 使用无服务端副作用的 `request-user-input` 客户端工具只处理核心语义歧义，单次只问一个问题、每个用户请求最多询问一次；持久化等待态，并在回答、跳过或关闭后通过类型化工具输出自动续跑。
- [x] 历史版本曾支持 `publish-research-plan` 专用组件；P2 后仅保留旧消息渲染，新运行不再注册该工具。
- [x] 历史版本曾强制研究计划门禁；P2 后问题拆分属于 Research Skill 方法，不再由运行时阶段状态机推进。
- [x] 通过 AI SDK `onStepEnd` / `onEnd` 持久化每轮完成原因、输入/输出/推理与缓存读写 token、步骤/工具计数和耗时；取消/中断仍保留状态与可得的已完成步骤指标。
- [ ] 为长会话预留上下文裁剪或摘要字段，但本阶段不自动改写用户消息。

### 验收门槛

- [ ] 可完成至少十轮连续对话，文本逐步出现，停止后不再收到增量。
- [ ] 失败消息可重试，重新生成不会覆盖原回复，应用重启后历史仍可读取。
- [ ] 普通对话的出站请求不包含工作区路径、文件名、当前文档内容或其他未显式选择的数据。
- [ ] 输入法组合、长文本粘贴、代码块、取消请求和组件卸载均有自动化或可重复的手工验证步骤。

## M2：只读工作区 Agent

目标：用户进入工作区任务或打开文档侧栏后，系统在内部使用 Agent，使模型可以分步骤读取并理解工作区，但不能自行改变文件。

### 工具与编排

- [x] 使用 AI SDK `ToolLoopAgent` 实现工具循环，并通过 `inputSchema` 校验入口输入。
- [x] 第一批工具限制为 `list-workspace-files`、`read-workspace-file`、`search-workspace-text` 和 `read-current-document`。
- [x] 主进程为每次运行注入工作区根目录；所有工具统一执行路径归一化、目录穿越和符号链接逃逸检查。
- [x] 限制单文件大小、搜索结果条数、工具输出体积、总步骤数、总耗时和 token 用量；Markdown 读取默认按 400 行、单次最多 50 KiB 分页，达到限制时返回结构化范围与 `nextOffset`。
- [x] 将 AI SDK 工具增量通过泛型 `AgentRuntime` 端口输出，保留推理、工具、审批、完成、失败和取消的 `UIMessageChunk` 语义。

### 界面与可见性

- [x] 在提交前明确展示本次 Agent 可读取的工作区和数据范围。
- [x] 消息中展示工具名称、状态、读取文件和失败原因；冗长结果不展开到消息正文，但实际访问的相对路径保持可见。
- [x] 回答中的工作区结论支持相对 Markdown 链接跳转到文件与源码行号。
- [x] Agent 与普通对话使用任务级明确的内部状态，不根据提示词自动提升权限；界面不暴露运行时选择，缺少工作区或工具能力时阻止发送而不降级。

### 验收门槛

- [ ] Agent 能通过多次工具调用回答跨文件问题，并为关键结论给出可验证的文件来源。
- [x] `../`、绝对路径、工作区外符号链接、超大文件和超量搜索结果均被主进程拒绝或截断为结构化受限结果。
- [x] 只读工作区运行时没有可达的写入、删除、重命名、Shell 和任意网络接口；供应商原生联网仅由创作方式和已验证端点显式注入，MCP 通过独立信任与逐次审批边界注入。
- [ ] 停止 Agent 后，正在执行的工具和后续步骤都能终止，并产生完整的结束事件。

## M3：可写工作区 Agent

目标：Agent 可以提出 Markdown 修改，但最终写入权仍属于用户和主进程。

### 修改提案与批准

- [x] 增加 Markdown 创建和更新工具；工具输入包含相对路径、完整候选内容、修改理由和读取时的基准版本。
- [x] 使用 AI SDK `toolApproval` 暂停写工具；主进程在公开审批事件前保存不可变候选输入。
- [x] 在审批卡片中展示目标路径、Markdown 渲染结果、逐行高亮 Diff、增删统计与基准版本。
- [x] 用户批准后，主进程重新校验工作区路径、冻结提案、内容哈希和磁盘版本，再原子落盘。
- [x] 用户拒绝时由 AI SDK 生成 `output-denied` 并继续工具循环；系统提示禁止无新依据重复请求。
- [x] 磁盘版本变化时禁止写入，保留候选内容并返回结构化冲突，不自动覆盖外部修改。

### 审计与状态同步

- [x] 记录任务、运行、模型、工具、目标路径、冻结基准/候选、用户决策、结果和时间；不记录供应商密钥。
- [x] 写入完成后由工作区观察器刷新文件树；当前文档有草稿时沿用编辑器外部冲突流程。
- [x] 待审批提案和流事件持久化；窗口刷新/应用重启可恢复预览，取消或重启不会自动执行写入。
- [ ] 删除、重命名、批量改写、非 Markdown 文件和 Shell 保持不可用，直到有独立的权限与恢复设计。

### 验收门槛

- [x] 「修改 README」只生成可审查 Diff；批准后文件原子更新，拒绝后磁盘内容不变。
- [x] 审批期间发生外部修改时，写入被阻止且不会丢失用户版本或候选版本；同一基准版本的进程内并发批准更新按文件串行复核，后到更新返回冲突。
- [x] 每次批准、拒绝、冲突和失败均可在会话与审计记录中追踪。
- [x] 应用异常退出后不存在半写文件；恢复后不会自动执行未完成的批准请求。

## M4：统一创作 Agent 与存储探索

目标：在不改变安全边界的前提下消除任务级 Chat/Agent 分叉，让同一会话自然完成问答、研究、写作、创建产物和整理项目；用后端无关的领域工具评估最终存储方案。

### 统一运行与每轮策略

- [x] 在编码前针对锁定的 AI SDK 版本完成能力矩阵，至少核对 `ToolLoopAgent`、call options / `prepareCall`、`prepareStep`、`stopWhen`、`toolApproval`、typed UI messages、ChatTransport、生命周期和子 Agent；能力矩阵记录在[统一创作 Agent 与内容存储探索](unified-creation-agent.md)，依据 `ai@7.0.73` 随包 docs/source。
- [x] 让无工作区任务也通过 AI SDK `ToolLoopAgent` 运行；无资源且无需搜索时允许零工具直接生成文本。
- [x] 定义第一版受信任 `RunPolicy`：主进程按本轮显式创作方式、内部作用域和持久化模型事实解析实际端点、Skill、联网、推理、工具作用域及安全护栏；renderer 只做同源预检，IPC 不接受 `reasoning` / `webSearch` 命令。研究不设置累计 Token 或应用侧单步输出额度，只按模型上下文设置宽松循环保险丝。无工作区与工作区路径共用 `task-agent.ts` 的 `callOptionsSchema` / `prepareCall`，不复制 Agent loop。
- [x] 在第一版 RunPolicy 上增加保守的用户 turn 自动研究/写作识别、规范化动态资源和领域工具审批；自动化只收窄主进程已经确认的资源授权。更强语义路由与细粒度策略仍继续演进。
- [x] 把实际 Skill、完整 RunPolicy 和资源摘要固化到 `task_runs`：`0009-task-run-policy` 保存兼容查询列，`0010-task-run-context` 保存 `policy_json` / `resource_summary_json`，旧运行保持未知值；后续以关系表替代仅有摘要的动态资源表达。
- [x] 允许自动/研究/写作/问答在每轮发送前切换；同一任务可以从普通问答进入研究和写作，旧 run 不被回写。
- [x] 通过 `prepareCall` 返回 `activeTools`、reasoning、instructions、`stopWhen` 和输出上限，实现逐轮工具/预算收窄；后续只有确需 step 内继续缩小时才增加 `prepareStep`，任何 step 不能扩大 RunPolicy。
- [x] 通过 AI SDK 工具 `needsApproval` 表达 Markdown 写入与 MCP 暂停；Tessera 只持久化审批、Diff 和审计，不自建平行工具状态机。更细的逐轮审批矩阵后续可由 `prepareCall` 返回 `toolApproval`。
- [x] Agent 运行时使用 `InferAgentUIMessage<typeof agent>` 推导工具消息，历史交给 `createAgentUIStream` 内部标准 `validateUIMessages` / `convertToModelMessages`；React 继续复用 `useChat`，工具呈现统一走 AI SDK `isToolUIPart` / `getToolName`，应用 metadata/data 仅通过受控 schema 扩展。
- [x] 使用 AI SDK 生命周期回调记录 step、工具耗时、usage 和 finish reason，并按 request ID 关联现有 `task_run_events`；SQLite 保存无正文汇总，开发期细节由官方 DevTools 查看，不从 UI chunk 反推。
- [x] 使用同一薄 `AgentRuntime`、消息 Part、暂停/续跑、审批、取消和运行恢复协议，停止为 Chat 和 Agent 维护两条产品运行链路。
- [x] Electron transport 只实现标准 `ChatTransport` 的 IPC/重连差异；`DirectChatTransport` 不进入生产链路，也没有另写 React chat 状态机。

### 研究工作流

- [x] 按[研究工作流与证据链](research-workflow.md)定义并实现研究计划、问题、来源、读取、证据、覆盖和完成汇总的最小契约。
- [x] 让显式研究进入可恢复的领域阶段，首次搜索前必须有计划；自动方式的原子事实问答继续允许零计划直接回答。
- [x] 实现受限 `read-web-source`，将候选 URL 明确归一化为已阅读或不可用，不能把搜索摘要计为页面阅读；桌面端复用系统代理，静态读取失败时回退到无登录态沙箱浏览器。
- [x] 增加声明到来源片段的证据关系和领域完成检查；实际材料不可访问、冲突或合理尝试仍不足时保留部分完成、未覆盖问题和限制。
- [x] 用真实事件分别呈现搜索、发现、阅读、引用、推荐与保存数量；推荐来源只有在用户明确选择后才沉淀为材料 Artifact。

### 资源与领域工具

- [x] 定义后端无关的 `ProjectRef`、`DocumentRef`、`ArtifactRef` 与 `ResourceBinding` 公共契约；不暴露数据库表、绝对路径或正文。Operation 与领域工具输入随首个内容服务实现补齐。
- [x] 实现 `list-projects`、`create-document`、`create-project`、`move-documents` 和 `inspect-project` 的受限应用服务。
- [x] 当前混合实验增加用户选择的内容库根目录和可见“未归档”Workspace；保留内容库外普通工作区，不静默搬迁用户目录。
- [x] 文档创建后在同一 TaskPage 显示 Artifact，并可打开文档 + 原任务侧栏；项目移动追加当前 scope，不复制任务历史。
- [x] 创建与移动执行冲突预检、AI SDK 人工审批、审计和恢复信息；覆盖、删除与 Shell 保持不可达，MCP 继续独立逐次审批。
- [x] 将同一回复中的创建文档、创建项目、移动文档和结构检查聚合为一条 Operation 活动；完成态隐藏内部 ID，
  等待审批或失败时仍保留 AI SDK 标准工具状态与审批界面。

### 存储评审

- [ ] 用同一组端到端场景分别评估数据库正文、托管内容库混合方案和完全开放外部工作区，不让 UI 与工具协议绑定实验后端。
- [ ] 记录首次设置成本、任务成功率、操作失败率、外部编辑冲突、导入导出、备份恢复、多端需求和迁移成本。
- [ ] 在任何正文事实源迁移前形成独立 ADR，明确权威副本、事务/恢复、备份、导出和旧数据迁移；评审前禁止数据库/Markdown 双写。

### 验收门槛

- [ ] 同一任务完成“普通问答 → 联网研究 → 写作 Skill → 创建文档 → 建立项目 → 移动文档”，全程没有 Chat/Agent 选择。
- [x] 当前混合适配器已自动化覆盖“普通问答 → 研究计划/搜索/页面读取协议事件 → 写作 Skill → 创建文档 → 建立项目 → 移动文档 → 重启恢复/中断收口”；完整联网研究的来源质量与证据链仍由研究工作流验收约束。
- [x] 每个完成消息保存本轮 `requestId`；归属校验后的只读 IPC 与按需 popover 可解释实际模型、Skill、资源、工具及结束/失败原因，切换创作方式不改变旧 run 的权限语义。
- [ ] 当前混合实验退出 Tessera 后仍能直接打开全部 Markdown；删除 SQLite 索引不会删除或覆盖正文。
- [ ] 最终存储评审可以替换适配器而不改写核心对话、领域工具和活动 UI 契约。

## 横向任务

- [x] 为 AI Chat 模型运行、工具输入/执行和恢复 IPC 建立版本化公开错误码；运行失败使用 `data-task-error`，工具失败保留标准 Tool Part 并并行写入 `data-tool-error`，界面文案不暴露密钥或内部堆栈。其余非 Agent IPC 仍按各领域契约处理。
- [x] 启动后失败也先写入同一运行事件；重复/乱序/缺口事件、幂等停止、页面重连与应用重启中断统一收口，恢复不重放已经执行的磁盘副作用。
- [ ] 首输出延迟、总耗时、token 用量和工具次数已按 run 本地保存；取消率与统一失败原因聚合查询仍待实现。
- [x] 为路径攻击、审批批准/拒绝、版本冲突、完整工具历史和运行事件持久化建立自动化回归；普通问答与写作质量评测仍需扩充。
- [ ] 为写作质量建立人工评测集，分别评价事实保持、语气、结构、修改幅度和引用可靠性；不只比较单次演示效果。
- [ ] 当消息协议、权限模型或数据库事实源变化时，同步 `architecture.md`、相关目录 `.folder.md` 和源文件 `[DOC]`。
- [ ] 每次升级 AI SDK 先阅读锁定版本随包 docs/source、运行类型检查和 Agent/transport/approval 回归；实验性 API 必须有隔离边界与退出路径。

## 建议交付顺序

每项保持为可单独审查和回退的变更：

1. 供应商安全存储、配置 IPC 与默认模型解析。
2. 流式生成 IPC、取消和模拟供应商测试。
3. 对话界面、通用会话模型与历史恢复。
4. 只读 Agent 工具、路径安全和文件引用。
5. Diff 审批、版本复核、原子写入和审计。
6. 统一 `ToolLoopAgent` 与每轮 RunPolicy，保留旧任务兼容。
7. 研究 P1 来源推荐/保存与工作文档、P2 浏览器读取后备。
8. 动态资源、Artifact 和后端无关的项目/文档领域工具。
9. 托管内容库混合实验与三种存储方案评审。
10. 上下文控制、成本可见性、评测集和可靠性收尾。

M0 验收前不开始真实对话接入；M1 的流式协议稳定前不接工具事件；M2 的路径与资源限制通过测试前不开放写工具；M3 的 Diff、批准和冲突处理缺少任一项时，不允许 Agent 修改磁盘；M4 存储评审完成前不迁移正文事实源或建立双写。
