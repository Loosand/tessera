# Tessera 设计规范

> 状态：持续演进。本文是 Tessera 产品界面、交互语言与设计系统的判断依据。

## 文档职责

本文回答 Tessera 应当如何组织信息、呈现状态和处理交互，不记录某次视觉改版或组件实现细节。

- 产品目标、能力范围与阶段状态以 [产品边界](docs/product.md) 为准。

- 运行时、数据流与安全边界以 [系统架构](docs/architecture.md) 为准。
- 编辑器协议与性能阈值以 [编辑器架构](docs/architecture/editor.md) 为准。
- 颜色、字体、间距和圆角的当前值由 `packages/design-system/styles/globals.css` 持有。

参考产品用于校准判断，不复制其品牌标识、组件命名、页面结构或主题机制。

## 产品体验

Tessera 是本地优先的阅读、研究与写作工作台。用户面对的核心对象是材料和 Markdown 文档，不是 Agent、
数据库或工作流配置。

编辑器在没有模型时必须完整可用。AI 在用户需要时围绕材料、选区和文档提供建议，并通过引用、权限和 Diff
建立信任。无论内容来自人工输入还是 AI 建议，最终都回到同一份可读、可编辑、可导出的本地文档。

产品气质应当安静、直接、可靠。界面通过清晰层级、稳定布局和可解释状态建立信心，不通过装饰、拟物或夸张反馈
制造存在感。

## 冲突优先级

需求发生冲突时，按以下顺序保护：

1. 文档安全、用户意图、权限边界与可撤销性。
2. 不依赖 AI 的阅读、写作、保存和导出路径。
3. 当前任务、正文内容与关键状态的清晰度。
4. 设计系统的一致性、可访问性和平台惯例。
5. 响应式细节、动效和视觉精修。

不得为了保持某个布局、组件或视觉效果，牺牲内容完整性、可理解性或操作可逆性。

## 设计工作方法

1. **明确任务**：判断用户此刻在读什么、写什么、确认什么；区分正文、来源事实、用户判断、Agent 推断和系统状态。
2. **选择结构**：先确定主要工作表面和阅读顺序，再选择列表、表格、正文、Diff 或对话等表达形式。
3. **使用系统**：页面决定构图、密度和信息顺序，但不建立平行的颜色、字号、圆角、阴影或控件体系。
4. **检查结果**：在真实应用中验证首屏、完整流程、窄窗口、明暗主题、键盘路径和长内容，先修复系统性问题。

快速路径通过标题、当前对象和关键状态建立方向；审查路径保留原文、引用、Diff、权限范围和历史记录。审查信息可以
后置或按需展开，但不能省略。

## 视觉系统

### 应用框架与布局

- 桌面框架由内嵌导航侧栏、主要画布、文档顶栏和按需出现的辅助面板组成；不增加横跨整个窗口的统一 Header。
- macOS 侧栏收起时，顶栏左侧操作避开交通灯安全区，侧栏开关位于三颗交通灯右侧；其他平台不保留无意义的固定空白。
- 主区域顶栏不使用分割边框；侧栏收起按钮保持可见，不通过悬停反复显隐。
- 正文或当前任务始终是视觉主角；只有侧栏使用四周留白、圆角和弱描边形成内嵌容器，主区域直接作为无描边窗口画布。
- 默认空间是不可删除的虚拟 Space，不对应磁盘目录；侧栏顶部切换默认空间与最近文件工作区，应用记住上次 Space。侧栏主体不重复罗列工作区：连续显示当前 Space 的最近活动任务，默认读取二十条，之后每次“加载更多”再追加二十条，并只在标题右侧提供轻量“查看全部任务 ›”入口；置顶任务排在普通任务之前，菜单项右侧直接提供置顶与归档按钮。文件 Space 中任务区最多占可用高度的 45% 并内部滚动，文件树占据剩余高度独立滚动，不能被任务长列表推离可见范围。“全部任务”页面每页十条、占满可用高度且分页栏固定在底部，并提供已归档视图和恢复操作；分页与活动/归档过滤必须在服务端完成，不能只对固定截断的前若干条做假分页。
- 文件 Space 不进入任务/文件二级页面，也不替换一级侧栏。打开任务或文档只替换无描边主画布；文件树使用留白、弱化标签和缩进组织连续导航，只有目录保留折叠指示槽，叶文件从所属层级的自然起点开始，不能为了不存在的展开按钮额外空出一列。任务与文件名称必须使用可收缩文本列，列表容器与行内都保留右侧安全区；任务行始终保留固定尾部状态轨道，运行时在其中显示 loader，结束后只隐藏 loader 而不改变标题宽度。
- 文件和文件夹的右键菜单使用紧凑单列命令布局，只暴露已接通的平台能力；新建继承目标目录，删除明确写为“移到废纸篓”并在执行前使用系统确认。
- 工作区和对话的右键菜单使用相同的紧凑命令密度，直接作用于被点击对象；删除对话必须确认，从最近列表移除工作区不得删除磁盘内容或关联对话。
- 设置侧栏的返回、搜索和分类项共享固定图标列与文本列；不同 glyph 在等宽槽位内居中，不能改变文字起始位置。
- 侧栏底部只保留高频工具栏并延续同一背景；Space 与文件操作按对象收进紧凑菜单，不重复显示路径、版本等低价值元数据。
- 对象对齐到共享边缘、基线或网格；正文保持舒适行长，表格、Diff 和复杂证据可以使用更宽画布。
- 窄窗口优先重排、覆盖或收起辅助区域，不能压缩正文到不可读，也不能隐藏核心操作。

### 排版与节奏

- 界面和无衬线正文共享用户选择的字体，未选择时使用系统字体；代码、命令、路径和短标识符使用系统等宽字体。
- 通过有限且稳定的标题、正文、标签和辅助文字角色建立层级，不为单个页面发明任意字号或字重。
- 正文保持约 60–68 个字符的舒适行长；中文排版按实际视觉密度校正，不以缩小字体换取容量。
- 间距表达关系并由容器统一负责；重要换行通过文案、宽度或构图解决，不单独缩小元素。

### 颜色、表面与边界

- 主要界面由中性色构成。颜色只表达链接、焦点、选择、状态、警告、错误或数据差异，并同时提供非颜色线索。
- 模型目录用 `capability-*` 语义色区分上下文、推理、工具、联网、视觉与结构化输出；每种颜色必须同时配套图标与可访问名称。
- 应用默认是一张连续画布。只有选择、交互、警告、对比或真实对象分组值得使用表面与边界。
- 优先通过排版、对齐、留白和密度变化建立层级；边框、底色和阴影不能替代信息结构。
- 卡片只承载独立对象，禁止嵌套；亮暗主题共享相同语义和视觉层级。

### 控件与状态

- 主要操作靠近其影响的对象；全局操作保持固定位置。
- 使用原生语义控件和设计系统原语，提供可见焦点、键盘操作、禁用状态和明确名称；普通单选项统一使用原生 `select`、`option` 与 `optgroup`。模型等需要同时比较品牌、能力与选中状态的高信息密度对象使用分组浮层，触发器持续显示当前对象。
- hover、selected、pressed、saving、conflict、error 和 disabled 必须可区分，不能共用含糊反馈。
- 设置页按通用、外观、编辑器、快捷键、AI、模型供应商、MCP 与关于组织；分类与快捷键清单均提供本地搜索。编辑器设置中的 Markdown 主题构建器直接映射 Typeset 字体、节奏与版心变量，使用真实语义内容实时预览，并提供参考预设与基于精选本地字体、可读节奏的随机搭配。连续参数使用具有清晰轨道、进度、可见焦点和安全命中区的 Slider。
- 设置页采用常规桌面界面的字号与字重：页面标题仅保留适度强调，分组与设置项使用中等字重，说明文字保持紧凑、易读。
- 通用、外观、编辑器、快捷键、AI 与关于设置复用同一居中内容最大宽度，切换分类时左右边界保持稳定；模型供应商和 MCP 的目录/详情主从工作区是独立宽幅例外。
- 尚未接入运行时的设置必须禁用并说明规划状态；不能持久化一个不会影响产品行为的伪偏好。
- 破坏性操作提供确认、撤销或差异预览；动效只解释状态变化和空间连续性，并尊重减少动态效果设置。
- Motion 只承担侧栏和辅助面板重排、浮层进退场及共享选中态等结构性变化；正文输入、长列表和普通 hover 保持轻量 CSS，避免动效侵入编辑性能路径。

### Markdown 与长文阅读

- 即时预览编辑、静态渲染和 Markdown 源码属于同一内容系统，层级、宽度和语义状态保持一致；源码表面使用按需加载的 CodeMirror 6，复用 Typeset 等宽字体、字号、行高、版心与明暗语义色。
- Markdown 排版采用项目持有的 Typeset 节奏系统，以基础字号、行高和区块流三项变量派生标题与正文关系；用户可分别选择本地标题、正文和等宽字体并以 `ch` 调整版心，颜色、圆角和明暗模式继续来自 Tessera 语义 token。
- 标题、正文、列表、引用、代码、表格、图片和分隔线形成稳定的长文节奏，编辑状态不能破坏阅读结构。
- 表格用于精确查找与比较，列头和单元格对齐一致；长表格先获得足够宽度，再考虑局部横向滚动。
- Markdown 是内容事实源。视觉样式和编辑器运行时不能引入无法审查或导出的正文副本。
- 即时预览无法保真往返的语法默认留在 Markdown 源码模式，提示需要说明保护原因并允许用户明确覆盖。
- 本地版本历史对所有工作区可用，以自动检查点、比较和恢复描述文档变化；恢复旧版本会产生新的当前版本，不删除后续历史。
- 已有 Git 仓库可以提供变更树、文件 Diff、文件级暂存和提交；分支管理、rebase 与远程仓库工作流不进入首个版本。
- 即时预览以区块为交互单位：当前顶层区块共享一个按需出现的手柄，支持单块或连续多块选择、键盘导航、Markdown 复制/剪切、结构化粘贴、操作和重排；嵌套区块按编辑器协议继续演进。
- 编辑器区块菜单与斜杠命令使用高密度命令面板：固定图标槽、短标签、清晰分组和右侧语法或快捷键提示；说明文字只用于消除歧义，不把每条命令扩张成卡片。

### AI 协作与信任

- 编辑器是主要工作表面，Agent 面板是按需出现的辅助表面；面板与完整任务页复用同一会话实现和历史，关闭后不能损害写作流程。当前文档只有以可见、可移除的附件加入时才进入模型上下文，附件应使用编辑器最新草稿而不是静默读取旧磁盘版本。
- 「新任务」是主导航第一项。空任务页是当前 Space 的落地页：在同一条 600px 阅读栏中展示空间标识、居中的多行输入框和最多五个作用域最近任务；输入表面提供材料、上下文与权限浮层、模型和发送入口。任务开始后，用户消息占据与助手正文一致的阅读栏并使用整行浅色表面，助手正文保持在连续画布上，只有计划、研究进度、审批和 Artifact 等独立过程对象使用卡片。
- 文档侧边对话沿用同一任务协议，但不与文档主区域共享 Header：文档 Header 以右栏图形提供打开/关闭入口，不放设置按钮；侧栏作为完整高度的独立列，只保留“侧边对话”标题和关闭操作，任务新建与历史切换统一由一级 Space 侧栏承担。侧栏默认宽于旧窄栏，支持从左缘拖拽或用键盘调宽，遵守 380–720px 边界并记住用户选择；窄窗口转为右侧覆盖层。中间消息独立滚动，输入框固定在底部，空态不重复解释已经由附件表达的当前文档上下文。通过 Artifact 打开新项目文档时继续原任务，不因工作区资源变化静默重置会话。
  任务开始执行后进入可承载来源、过程和产物的工作布局，不长期停留在居中输入状态。
- 「技能」是一级本地管理入口，组合内置注册表与用户安装目录，呈现真实安装状态、来源、权限边界与按需加载方式；卡片列表保持完整内容宽度，详情点击后从右侧 Sheet 临时打开，不以常驻分栏挤压目录。页面允许从含标准 `SKILL.md` 的本地文件夹添加、启停、删除或带着已选 Skill 创建任务；删除托管副本使用系统废纸篓。用户 Skill 只注入 instructions，附带脚本不执行，工作区级安装和社区发现未接入前不得伪装成可用操作。
- 用户只选择创作意图，不选择 Chat/Agent、是否调用工具、联网开关或思考档位。所有任务使用同一 AI SDK Agent 动态配置：简单请求可以零工具直接回答，复杂请求按本轮意图和授权资源调用搜索、Skill 或领域工具。创作方式浮层提供「自动 / 研究 / 写作 / 问答」快捷项：未选择时为自动；研究和写作分别加载内置 Skill 并带出相应策略，问答只是离线回答预设，不伪装成 Skill。图片、视频等专用生成属于工具、输出能力或模型路由，未来可在同一面板分组呈现，但不与 Skill 互斥，也不形成新的会话模式。用户可在运行前后随时切换；已开始的运行保持冻结策略，新选择从之后发起的运行生效，实际 RunPolicy 固化到对应运行记录。旧 `chat` / `agent` 字段暂只表达内部资源作用域和历史兼容，不作为用户模式。缺少已验证能力或授权资源时阻止对应动作并说明原因，不能静默扩大权限。写工具必须在消息内展示文档渲染结果与高亮源码 Diff，并提供明确的拒绝/确认操作；批准前不改变现有正文。
- 无工作区任务可以先完成问答和研究；内容存储仍处于探索阶段，当前原型暂用托管内容库混合方案：设置页以“探索中”标识让用户选择、切换或撤销内容库授权，创建正式产物时把新 Markdown 放入可见的“未归档”工作区。用户要求建立独立项目后，Agent 通过受限领域工具创建本地目录、登记工作区、移动产物并继续原会话；消息区显示稳定 Artifact 卡片，点击后进入文档 + 原任务侧栏的左右协作视图。领域对象和活动轨迹不能绑定具体后端，后续仍需与数据库正文、完全开放外部工作区方案比较；最终方案确认前不得把实验路径伪装成稳定产品承诺。
- 对话输入首先是可靠的手动写作表面：多行编辑、选区、粘贴、撤销、中文输入法与键盘换行不能依赖 AI；主任务的消息流与输入表面使用同一条 600px 阅读栏。Composer 自己完整持有单一静态外轮廓和浅灰底栏，白色正文/操作面与底栏在同一裁切容器中分层，内部表面不得重复绘制完整边框，页面定位层也不得再增加只圆上角的第三层背景；空任务与运行中任务复用同一几何和对比度。聚焦或存在草稿时不改变容器边框、阴影与几何，只保留文字光标和内部控件自身的焦点可见性。常驻工具栏只保留附件、上下文与权限图标、创作方式、模型与发送；两个选择入口都使用胶囊 trigger，菜单容器使用大圆角、内部选中项使用与内边距匹配的次一级圆角。创作方式以闪光、搜索、编辑、对话等直接语义图标和一句短说明区分自动、研究、写作与问答，不使用脑形或浏览器造型代替抽象能力；模型 trigger 使用次级小字号，菜单中的名称和能力分两行呈现。当前文档和本地图片使用同一个附件入口，作用范围与写入审批规则只在点击图标后的 Popover 中展示，不把说明长文常驻塞入输入框。专业能力由创作方式、Skill、工具和模型事实共同派生，不为联网、工具调用或思考强度建立常驻开关。后台模型目录读取与同步保持静默，真实操作失败只占用输入框操作区的既有弹性空白并直接显示，不得与次级范围信息一起隐藏，也不得在输入框外增减状态行造成布局跳动；供应商明确返回的 reasoning 正文才进入整轮工作过程，并在受限高度的独立滚动区内以紧凑密度复用正文的安全 Streamdown 渲染。正文、reasoning 与 Agent 结果预览统一支持 GFM、CJK、Shiki、KaTeX 和 Mermaid；流式态由 Streamdown 外层的尾部缓冲只沿末尾逐帧揭示已经到达 renderer 的 grapheme，Streamdown 自身只修复未闭合语法而不重播字符动画，静态历史和组件重挂载均不得重新吐出旧内容，reduced-motion 直接显示完整增量。代码、表格和图表提供复制、下载或全屏等适用控件，原始 HTML 始终禁用；正文表格使用独立完整圆角外框，横向溢出只在内层滚动，首列默认保持单行，避免边框被滚动裁切或窄列拆字。空 reasoning 生命周期不单独刷屏。流式跟随不能打断用户向上阅读，也不能把未返回的内部思维链伪造成产品内容。
- 对话消息保留正文、来源与工具调用的 Part 边界，并分别表达生成中、完成和失败状态；正式回答前的 reasoning、过程说明、搜索、研究计划和信息性自动工具统一收进“已工作 x”区块，运行中展开并由 3×3 像素网格和文字 shimmer 表达活动状态，结束后动效静止并折叠，展开内容不绘制左侧时间线，内部继续使用结构化活动 UI 而不是面向用户渲染原始 JSON。请求用户回答、人工审批、拒绝、失败、产物与正式回答保留在区块外；消息流末尾不再重复常驻“正在处理”状态，也不把工具执行、后处理误称为“思考中”。联网搜索把真实查询、工具状态和去重来源聚合为可展开、限高的独立滚动过程轨迹，来源优先显示网站 favicon、失败回退通用图标，不用模型自述伪造搜索活动；普通工具调用使用紧凑 Tool Chips 呈现动作、目标和状态，文件变更摘要允许键盘聚焦并预览行级 Diff；流式正文逐段更新，不用重新拼接整条消息来模拟增量。
- 正常完成的回答在正文与消息操作之间呈现紧凑的“继续探索”，提供 2–4 个与结论相关、可直接发送的问题；研究回答优先指向证据缺口、争议、时间变化和深层机制。点击只把问题带入输入框，允许用户继续编辑，不自动发送；问题作为版本化消息 Part 恢复，不能由客户端临时随机生成。
- 完成回复的轻量操作栏常驻复制、赞、踩、按需用量/运行审计与适用时的重新生成；复制成功短暂切换为完成图标。用量浮层紧凑展示供应商/AI SDK 上报的总、输入、输出、推理和缓存读写 Token，步骤/工具数、分项耗时与请求 ID，并在存在 ContextManifest 时展示预计输入、安全预算、预留输出和观测步骤；本地估算必须明确区别于供应商计费。未返回值明示为未知，不伪造零值或价格，也不将可能重叠的缓存/推理口径再求和。赞踩允许改投或再次点击撤销，并以带时间的消息 metadata 仅保存在本机任务历史中，关联具体助手回复与 `requestId`；它当前用于用户自己的质量标记和后续本地评估，不上传、不触发模型训练，也不能用没有落库的感谢提示伪装成已收集反馈。
- 研究进度按真实 Tool Part 区分已发现、已阅读、不可用、证据、推荐与完成状态；从首个已读来源开始即可查看由持久化领域状态重建的增量笔记。推荐来源默认可选但不自动保存，用户明确点击后才创建材料 Artifact，并在原任务中继续用于写作。
- 模型只有在核心对象或目标存在多个互斥且同样合理的解释、上下文无法判断且猜错会让答案答非所问时，才通过具名客户端工具发布一个决定方向的结构化问题；平台、篇幅、风格、语气、受众、文章角度、输出格式、资料范围和个性化偏好缺失时必须采用合理默认值并直接执行。每个用户请求最多询问一次，工具调用结束当前模型请求，任务进入可恢复的等待输入状态，普通消息提交暂时让位于问题卡片。用户回答、跳过或关闭后，选择作为工具输出进入下一轮并自动续跑；历史记录收拢为已回答摘要。研究方式在多步检索前通过独立工具发布目标、范围、交付物与问题清单，计划、真实检索轨迹和最终答案分别呈现。上述控制协议只接受已知且经运行时校验的结构；通用 JSON 渲染仅用于未来受限的展示组件，不能决定暂停、权限或执行流程。
- 消息列表仅在用户仍贴近底部时自动跟随新内容；用户向上阅读后保持当前位置，并提供明确的“滚动到最新消息”入口。
- AI 设置控制全局可用性、工具权限与研究网页的网络路径；研究网络默认跟随系统代理，也可显式直连，设置只影响之后开始的研究运行，不接管模型供应商请求或浏览器登录态。模型供应商使用独立设置入口，外部 Agent 与本地模型在能力落地前不混入远程 API 连接。
- 模型供应商使用紧凑目录与常驻内容区的主从布局；内容区提供全部连接总览和单个连接详情，切换连接时回到详情顶部，模型工具栏保持可达，模型按已启用与未启用分组；OpenAI 兼容与 Anthropic 兼容是可添加多条具名连接的协议适配器，DeepSeek、Grok 与 OpenRouter 官方服务保持单例；首次同步的单模型目录自动启用，包含多个模型时默认全部停用，后续同步保留既有显式选择且新模型默认停用，工具栏提供作用于完整目录的全部启用与全部停用；供应商与每个模型必须显示可辨识图标，模型图标按完整 ID 匹配精选品牌且未知模型保留中性头像；详情预填官方 API 地址并允许兼容端点覆盖，公共默认目录自动加载，目录检查与模型同步就地反馈状态，模型接口由适配器推导且不要求用户维护；模型目录是连接的可选能力，兼容端点返回 404 或 405 时只提示不支持目录，不得判定推理连接失效，并继续允许手动添加完整模型 ID；模型同步、手动增删和启停成功后立即持久化并通知任务页，每次进入新任务视图时在后台重新请求所有已启用连接的模型目录并保存结果，空目录首次恢复时只自动启用第一个模型，已有显式启停选择不被覆盖；Base URL、模型与启用状态跨重启恢复，密钥只以主进程安全存储密文持久化且不得回传 renderer。
- MCP 使用紧凑服务器目录与常驻详情区的主从布局；设置图标打开连接编辑 Dialog，详情直接呈现传输、握手身份、加密变量状态与工具清单。连接或启用前必须显式确认信任，工具逐项开关即时持久化；环境变量和请求头只显示“已配置”而不回显内容。检测连接就地反馈状态并发现工具，所有已启用 MCP 工具在 Agent 中仍逐次显示标准人工审批，不能用“只读”提示跳过确认。
- AI 建议、来源证据、权限请求、执行过程和最终写入使用不同语义，不伪装成普通正文或系统事实。
- 建议需要说明修改范围和依据；用户可以接受、拒绝和撤销，Agent 不能静默覆盖正文。
- 外部调用说明接收方与发送范围，文件写入前提供 Diff；Agent 不可用时人工路径继续完整工作。

## 组件架构

```text
tokens
  → primitives
  → base
  → patterns
  → features
```

- `tokens` 定义跨主题的颜色、排版、间距、形状、阴影和动效语义。
- `primitives` 封装 Base UI 等无样式行为原语，不携带产品状态。
- `base` 提供 Button、Input、NativeSelect、Popover、Switch、MessageScroller 等通用控件。
- `patterns` 组合 Sidebar、Toolbar、SettingSection、EmptyState 等重复界面关系。
- `features` 负责 Reader、Editor、Library、Research、Agent 和 Diff 等产品语义。

业务应用只从 `@tessera/design-system` 的明确子路径消费共享能力。Tailwind 负责样式组合，TipTap 负责富文本
编辑行为；具体依赖、能力状态和数据协议记录在对应目录文档与架构文档中，不在本文维护版本清单。

## 可访问性与响应式

- 使用正确的标题顺序、landmark、列表、表格和表单语义；源代码顺序即阅读顺序。
- 满足 WCAG AA，对比度、焦点和状态不能只靠颜色表达。
- 所有核心流程支持键盘；图标按钮具有可访问名称，装饰图标不进入阅读顺序。
- 内容先重排再缩小，不以隐藏 overflow 掩盖错误；明暗主题、窄屏、长文本、中文输入法和减少动态效果都需验收。

## 验收顺序

1. **任务与结构**：当前对象、主要操作和阅读顺序是否立即清楚？
2. **排版与克制**：角色、基线、行长和节奏是否稳定，是否仍有可删除的装饰？
3. **状态与恢复**：保存、冲突、错误、权限和 AI 建议是否清楚且可逆？
4. **适配**：明暗主题和窄窗口是否保持层级且没有溢出？
5. **信任**：来源、Diff、权限范围和数据去向是否明确？

## 明确拒绝

- 用渐变、发光、玻璃、纹理、拟物纸张或装饰阴影制造风格。
- 默认居中大标题后堆叠卡片网格，或让所有页面拥有相同轮廓。
- 为普通标签和元数据滥用 badge、胶囊、彩色图标底板和全大写眉题。
- 用嵌套卡片、重复边框和大片底色修补薄弱层级。
- 使用过小的灰色文字、任意字号、混乱字重、图标代替清晰文字或无法解释的留白。
- 把对话框当成主要工作区，把 AI 输出当成高于用户文档的内容事实。
- 通过隐藏溢出、截断表头或压缩正文维持布局，或添加不能解释状态与关系的动效。

Tessera 的克制不是只有黑白、细线和空白，而是让内容、任务、证据和操作关系比界面装饰更清楚。
