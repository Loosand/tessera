# Tessera 根目录

## 地位

仓库入口，维护工作区、全局工程规则、设计事实源与架构文档入口。

## 逻辑

`apps/` 组合可运行产品，`packages/` 提供可复用能力，`docs/` 记录产品边界和跨目录机制；根配置统一调度它们。

## 约束

- 依赖方向由应用指向包，包之间通过公开导出协作。
- 全局设计遵循 `design.md`，协作遵循 `AGENTS.md`。
- 格式化与 lint 只使用 Biome。

## 成员清单

- `apps/`：可运行应用。
- `packages/`：共享领域与 UI 包。
- `docs/`：产品边界与系统架构。
- `.agnets/`：项目内技能参考，不属于产品运行时代码。
- `AGENTS.md`：仓库协作约定。
- `design.md`：UI 与交互事实源。
- `README.md`：项目入口。
- `biome.json`、`turbo.json`、`tsconfig.base.json`：全局工程配置。

## 触发器

根级目录、工作区边界、全局命令或事实源发生变化时更新本文。
