# apps

## 地位

存放 Tessera 的可运行应用入口。

## 逻辑

应用负责组合共享包与平台运行时，不向共享包泄漏平台专有依赖。

## 约束

- 应用可以依赖 `packages/`，共享包不得反向依赖应用。
- 平台安全边界必须在应用入口处显式建立。

## 成员清单

- `desktop/`：Electron 桌面应用。

## 触发器

新增应用、删除应用或应用职责变化时更新本文。
