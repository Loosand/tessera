# main

## 地位

Electron 主进程入口，持有窗口生命周期与平台能力。

## 逻辑

启动时注册 IPC、创建安全窗口，并将外部导航交给系统浏览器处理。

## 约束

- 窗口保持 `contextIsolation` 与沙箱开启，禁止 Node.js 集成。
- 新增 IPC 前先在 `@tessera/contracts` 定义契约。

## 成员清单

- `index.ts`：主进程启动、窗口配置与 IPC 注册。

## 触发器

窗口策略、应用生命周期、IPC 处理器或成员文件变化时更新本文。
