# desktop/src

## 地位

桌面应用的三个运行时边界。

## 逻辑

`main/` 创建窗口并持有平台能力，`preload/` 建立安全桥，`renderer/` 负责界面呈现。

## 约束

- 跨进程数据必须经过共享契约。
- 预加载接口保持最小，不暴露通用 IPC 或 Node.js 对象。

## 成员清单

- `main/`：Electron 主进程。
- `preload/`：上下文隔离桥接层。
- `renderer/`：React 渲染层。

## 触发器

运行时目录、IPC 方向或进程职责变化时更新本文。
