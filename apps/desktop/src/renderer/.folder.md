# renderer

## 地位

Electron 渲染层的 HTML 入口与 React 应用目录。

## 逻辑

`index.html` 提供挂载点，`src/` 初始化并渲染 React 应用。

## 约束

- 不启用 Node.js 集成。
- 所有平台能力通过 `window.tessera` 窄接口访问。

## 成员清单

- `index.html`：渲染层 HTML 入口。
- `src/`：React 源码与样式。

## 触发器

HTML 入口、渲染框架或安全桥使用方式变化时更新本文。
