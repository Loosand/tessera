{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@tessera/agent-evals",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "report": "bun src/cli.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@tessera/contracts": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
