{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@tessera/skills",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@tessera/agent-runtime": "workspace:*"
  }
}
