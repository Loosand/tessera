# skills

## 地位

可移植 `SKILL.md` 工作流的发现与加载契约包。

## 逻辑

描述 Skill 的来源、作用域和所需权限，实际文件访问由后续适配器实现。

## 约束

- Skill 声明不等同于权限批准。
- 本包不执行外部命令或直接读取工作区。

## 成员清单

- `src/`：Skill 领域契约。
- `package.json`：包元数据、依赖与公开入口。
- `tsconfig.json`：类型检查配置。

## 触发器

Skill 模型、权限关系、公共入口或成员变化时更新本文。
