# skills/src

## 地位

Skill 发现与加载契约的源码入口。

## 逻辑

用描述符统一表达用户级和工作区级 Skill，以及其声明的资源权限。

## 约束

- 文件名常量与 `SKILL.md` 协议保持一致。
- 权限效果复用 `agent-runtime` 的共享类型。

## 成员清单

- `index.ts`：Skill 文件名、描述符和权限类型。

## 触发器

契约文件增删、Skill 作用域或权限结构变化时更新本文。
