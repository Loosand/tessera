# agent-runtime

## 地位

Tessera 核心与具体 Agent 实现之间的可替换运行时边界。

## 逻辑

调用方提交请求并消费适配器真实公开的异步事件流；工作区文件端口使模型工具定义不依赖本地 `fs` 实现。

## 约束

- 接口不绑定 OpenCode 或任一模型供应商。
- 权限语义必须保留明确资源范围。

## 成员清单

- `src/`：运行时端口和工作区文件能力契约。
- `package.json`：包元数据与公开入口。
- `tsconfig.json`：类型检查配置。

## 触发器

运行时协议、事件模型、权限语义或成员变化时更新本文。
