# agent-runtime/src

## 地位

Agent 运行时公共契约的源码入口。

## 逻辑

具体适配器以泛型请求进入可替换运行时，执行过程以保留领域类型的异步事件流返回；默认类型仍提供基础会话和权限事件。

## 约束

- 事件必须可序列化或可明确适配。
- 取消通过 `AbortSignal` 传播，不定义隐式全局状态。

## 成员清单

- `index.ts`：默认请求、基础事件、权限类型与可由具体适配器收窄的泛型运行时接口。

## 触发器

协议文件增删、事件类型或运行时接口变化时更新本文。
