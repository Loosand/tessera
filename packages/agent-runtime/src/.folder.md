# agent-runtime/src

## 地位

Agent 运行时公共契约的源码入口。

## 逻辑

请求进入可替换运行时，执行过程以类型化异步事件流返回。

## 约束

- 事件必须可序列化或可明确适配。
- 取消通过 `AbortSignal` 传播，不定义隐式全局状态。

## 成员清单

- `index.ts`：请求、事件、权限与运行时接口。

## 触发器

协议文件增删、事件类型或运行时接口变化时更新本文。
