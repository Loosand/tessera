# hooks

## 地位

设计系统中可复用交互 Hook 的预留目录。

## 逻辑

Hook 只封装跨组件复用的界面行为，不读取业务状态，也不直接访问桌面主进程能力。

## 约束

- 文件名使用 kebab-case。
- 导出的 Hook 使用 `use` 前缀。
- 只有出现两个以上真实使用方时才抽取公共 Hook。

## 成员清单

当前无成员。

## 触发器

新增、删除或调整公共 Hook 职责时更新本文。
