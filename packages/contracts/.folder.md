# contracts

## 地位

跨运行时共享的 IPC 与领域类型契约包。

## 逻辑

主进程、预加载层、渲染层和核心包共同依赖这里的稳定数据形状。

## 约束

- 不包含平台实现或副作用。
- 破坏性契约变化必须同步所有调用方。

## 成员清单

- `src/`：共享契约源码。
- `package.json`：包元数据与公开入口。
- `tsconfig.json`：类型检查配置。

## 触发器

公共契约、导出入口、依赖或成员变化时更新本文。
