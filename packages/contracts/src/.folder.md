# contracts/src

## 地位

共享契约包的源码入口。

## 逻辑

单一入口集中导出 IPC 频道、窗口生命周期、AI 配置/连接/对话和其他跨进程类型，后续按领域增长时再拆文件。

## 约束

- 类型应可在 Electron、浏览器和 Node.js 环境共同引用。
- 不导入具体运行时实现。

## 成员清单

- `index.ts`：IPC 频道、AI 配置/模型目录/流式对话与桌面 API 类型。

## 触发器

契约文件增删、重命名或导出职责变化时更新本文。
