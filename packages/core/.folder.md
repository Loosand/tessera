# core

## 地位

与平台无关的 Tessera 应用核心包。

## 逻辑

消费共享契约并向各应用提供纯领域数据和服务。

## 约束

- 不直接依赖 Electron、DOM 或 Bun 专用 API。
- 平台能力通过显式端口注入。

## 成员清单

- `src/`：核心领域源码。
- `package.json`：包元数据、依赖与公开入口。
- `tsconfig.json`：类型检查配置。

## 触发器

核心边界、平台依赖、公共入口或成员变化时更新本文。
